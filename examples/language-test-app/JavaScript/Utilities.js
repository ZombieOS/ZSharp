export function greet(name) {
  return `Hello from JavaScript, ${name || "Z# developer"}!`;
}

export function add(left, right) {
  return left + right;
}

export function isReady() {
  return true;
}
